<?xml version="1.0" encoding="UTF-8"?>
<ivysettings>
  <property name="ivy.local.default.root"             value="${ivy.default.ivy.user.dir}/local" override="false"/>

  <resolvers>
    <filesystem name="local">
      <ivy pattern="${ivy.local.default.root}/[organisation]/[module]/[revision]/[type]s/[artifact](-[branch])-[revision].[ext]"/>
      <artifact pattern="${ivy.local.default.root}/[organisation]/[module]/[revision]/[type]s/[artifact](-[branch])-[revision].[ext]"/>
    </filesystem>
  </resolvers>
</ivysettings>